// Stub. "Open with" feature removed.
import { IContextualMenuItem } from "@fluentui/react";

export default function useOpenWithMenuItems(_fileDetails?: unknown): IContextualMenuItem[] {
    return [];
}
