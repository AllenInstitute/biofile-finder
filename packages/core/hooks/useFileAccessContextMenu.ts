// Stub. File-access context menu removed.
import * as React from "react";

export default function useFileAccessContextMenu(
    _filters?: unknown,
    _onDismiss?: () => void
): (evt: React.MouseEvent) => void {
    return () => {
        /* noop */
    };
}
