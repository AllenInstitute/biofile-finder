{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "typeCheck": "lerna run typeCheck",
    "version": "lerna version --yes --no-commit-hooks --exact --no-push"
  },
  "devDependencies": {
    "husky": "3.0.5",
    "lerna": "3.16.4"
  }
}
