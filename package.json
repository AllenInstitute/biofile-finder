{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "lerna run lint",
    "test": "lerna run test",
    "typeCheck": "lerna run typeCheck",
    "version:all": "lerna version --yes --no-commit-hooks --exact --no-push"
  },
  "devDependencies": {
    "husky": "3.0.5",
    "lerna": "3.16.4"
  }
}
